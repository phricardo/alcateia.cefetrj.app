import { Open_Sans } from "next/font/google";

export const type_first = Open_Sans({
  weight: ["300", "400", "600"],
  subsets: ["latin"],
  variable: "--type-first-opensans",
  display: "swap",
});
